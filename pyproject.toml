[project]
name = "wifi-mapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "speedtest-cli>=2.1.3",
    "textual>=8.2.8",
]

[project.scripts]
wifi-mapper = "wifi_mapper:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
